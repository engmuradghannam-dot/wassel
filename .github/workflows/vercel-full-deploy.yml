name: Vercel Full Deploy
# Disabled - using vercel-api-deploy.yml instead
on:
  workflow_dispatch:
    inputs:
      note:
        default: 'disabled'
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use vercel-api-deploy.yml"
