name: Deploy to Vercel
on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Vercel deploys automatically via GitHub integration. This workflow is manual-only."
