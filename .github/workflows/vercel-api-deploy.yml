name: Vercel API Deploy
# Replaced by vercel-deploy-final.yml
on:
  workflow_dispatch:
    inputs:
      note:
        default: 'disabled - use vercel-deploy-final'
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"
