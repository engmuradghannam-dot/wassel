name: Test All Endpoints

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Full Test
        run: |
          echo "=== 1. Health ==="
          curl -s https://wassel-cyj5.onrender.com/api/health

          echo ""
          echo "=== 2. Login ==="
          curl -s -X POST https://wassel-cyj5.onrender.com/api/users/login \
            -H "Content-Type: application/json" \
            -d '{"email":"eng.murad.ghannam@gmail.com","password":"Admin2026@"}'

          echo ""
          echo "=== 3. Register new user ==="
          curl -s -X POST https://wassel-cyj5.onrender.com/api/users/register \
            -H "Content-Type: application/json" \
            -d '{"name":"Test User","email":"testuser888@test.com","password":"Test1234","companyName":"Test Company"}'

          echo ""
          echo "=== 4. Google OAuth URL ==="
          curl -sv https://wassel-cyj5.onrender.com/api/auth/google 2>&1 | grep -E "location:|HTTP/"

          echo ""
          echo "=== 5. Check REACT_APP_API_URL in deployed JS ==="
          curl -s https://wassel-seven.vercel.app | grep -o 'chunk.*\.js' | head -3
