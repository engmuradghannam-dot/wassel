name: Test All Endpoints

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Login
        run: |
          echo "=== Health ==="
          curl -s --max-time 30 https://wassel-cyj5.onrender.com/api/health

          echo ""
          echo "=== Login ==="
          curl -s --max-time 30 -X POST https://wassel-cyj5.onrender.com/api/users/login \
            -H "Content-Type: application/json" \
            -d '{"email":"eng.murad.ghannam@gmail.com","password":"Admin2026@"}'
