name: Test All Endpoints
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test chat-contacts
        run: |
          LOGIN=$(curl -s -X POST https://wassel-cyj5.onrender.com/api/users/login \
            -H "Content-Type: application/json" \
            -d '{"email":"eng.murad.ghannam@gmail.com","password":"Admin2026@"}')
          TOKEN=$(echo "$LOGIN" | python3 -c "import sys,json; print(json.load(sys.stdin).get('token',''))")
          echo "Token: ${TOKEN:0:20}..."

          echo "=== chat-contacts ==="
          curl -s "https://wassel-cyj5.onrender.com/api/users/chat-contacts" \
            -H "Authorization: Bearer $TOKEN"

          echo ""
          echo "=== all users ==="
          curl -s "https://wassel-cyj5.onrender.com/api/users" \
            -H "Authorization: Bearer $TOKEN" | python3 -c "
          import sys,json
          d=json.load(sys.stdin)
          print('Count:', d.get('count', len(d.get('data',[]))))"
