name: Test All Endpoints

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Full Debug Test
        run: |
          echo "### Health" >> $GITHUB_STEP_SUMMARY
          HEALTH=$(curl -s --max-time 30 https://wassel-cyj5.onrender.com/api/health)
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "$HEALTH" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "$HEALTH"

          echo "### Login" >> $GITHUB_STEP_SUMMARY
          LOGIN=$(curl -s --max-time 30 -X POST \
            https://wassel-cyj5.onrender.com/api/users/login \
            -H "Content-Type: application/json" \
            -d '{"email":"eng.murad.ghannam@gmail.com","password":"Admin2026@"}')
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "$LOGIN" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "$LOGIN"

          echo "### Setup Endpoint" >> $GITHUB_STEP_SUMMARY
          SETUP=$(curl -s --max-time 30 -X POST \
            https://wassel-cyj5.onrender.com/api/setup/setup-admin \
            -H "Content-Type: application/json" \
            -d '{"name":"Murad","email":"eng.murad.ghannam@gmail.com","password":"Admin2026@"}')
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "$SETUP" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "$SETUP"
