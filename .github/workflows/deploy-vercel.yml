name: Deploy to Vercel (Legacy - disabled)
# Replaced by vercel-full-deploy.yml
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is replaced by vercel-full-deploy.yml'
        default: 'disabled'
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use vercel-full-deploy.yml instead"
